# Input files for the script generating the Experiments target's credentials.
# The script is currently run via a build phase in the Experiments target.
$(SRCROOT)/Experiments/GoogleService-Info-template.plist
$(HOME)/.configure/woocommerce-ios/secrets/GoogleService-Info.plist
# Add the script itself as an input, so the build system will know to run it
# if it changes
$(SRCROOT)/../Scripts/build-phases/generate-credentials-experiments.sh
