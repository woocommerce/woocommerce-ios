import SwiftUI

struct HubMenu: View {
    var body: some View {
        Text("Work in progress")
    }
}

struct HubMenu_Previews: PreviewProvider {
    static var previews: some View {
        HubMenu()
    }
}
